{
  "buildCommand": "cd fechamento_firebase && pnpm install && pnpm build",
  "outputDirectory": "fechamento_firebase/dist",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html",
      "status": 200
    }
  ]
}
